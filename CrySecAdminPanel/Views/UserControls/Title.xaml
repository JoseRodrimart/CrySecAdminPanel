<UserControl x:Class="CrySecAdminPanel.Views.UserControls.Title"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CrySecAdminPanel.Views.UserControls" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <WrapPanel Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Bottom">
            <TextBlock FontSize="25" Grid.Row="0" Margin="25,0,0,0" Text="{Binding Text}"/>
        </WrapPanel>

        <Separator Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="Auto" Background="Gray" />
    </Grid>
</UserControl>
